interface:
  display_name: "APITrace Integration (Android)"
  short_description: "Add the APITrace Android network recorder to Android apps"
  default_prompt: "Integrate APITrace into this Android app's real networking path, keep release behavior safe, configure redaction deliberately, and add a developer-only export flow."
